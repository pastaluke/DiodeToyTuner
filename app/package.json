{
  "name": "diodetoytuner",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Precision web remote for cheap BLE LED controllers — the GIMP/Audacity of LED control.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/web-bluetooth": "^0.0.21",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
